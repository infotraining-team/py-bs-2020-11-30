# import primes_fun
import tools.primes as p
from tools.primes import primes
import sys

if __name__ == "__main__":
    print(p.is_prime(1))
    #print(p.not_primes)
    print(primes(100))
    print(sys.argv)
